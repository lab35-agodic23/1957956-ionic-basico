import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'miApp-2005055',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
